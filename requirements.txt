# 运行时依赖 —— 精简显式声明，传递依赖由 pip 自动解析
# Python >= 3.10

# Web 框架
streamlit>=1.49
streamlit-antd-components>=0.3.2
plotly>=5.22

# REST API 网关（多端复用）
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
PyJWT>=2.8

# 数据层
sqlalchemy>=2.0.30
pydantic>=2.7
pydantic-settings>=2.3
python-dotenv>=1.0

# 认证
bcrypt>=4.1

# AI 提供商
openai>=1.40            # OpenAI 兼容接入（SiliconFlow / Qwen / GLM / DeepSeek / Ollama ...）
google-genai>=1.0       # Gemini（新一代统一 SDK）

# RAG 向量检索
chromadb>=0.5.15

# 文档与图像
python-docx>=1.1
Pillow>=10.3
